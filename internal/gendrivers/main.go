// Command gendrivers generates driver imports from hurd.conf.
package main

import (
	"bufio"
	"fmt"
	"go/format"
	"log"
	"os"
	"strings"
)

func main() {
	log.SetFlags(0)
	log.SetPrefix("gendrivers: ")

	conf := "hurd.conf"
	if len(os.Args) > 1 {
		conf = os.Args[1]
	}
	f, err := os.Open(conf)
	if err != nil {
		log.Fatal(err)
	}
	defer f.Close()

	var paths []string
	seen := map[string]bool{}
	sc := bufio.NewScanner(f)
	line := 0
	for sc.Scan() {
		line++
		s := strings.TrimSpace(sc.Text())
		if s == "" || strings.HasPrefix(s, "#") {
			continue
		}
		if i := strings.Index(s, "#"); i >= 0 { // trailing comment
			s = strings.TrimSpace(s[:i])
		}
		if strings.ContainsAny(s, " \t\"`") || strings.Contains(s, "@") {
			log.Fatalf("%s:%d: %q is not a bare package import path "+
				"(pin versions with `go get pkg@version`, not in hurd.conf)", conf, line, s)
		}
		if seen[s] {
			log.Fatalf("%s:%d: duplicate driver %s", conf, line, s)
		}
		seen[s] = true
		paths = append(paths, s)
	}
	if err := sc.Err(); err != nil {
		log.Fatal(err)
	}

	var b strings.Builder
	b.WriteString("// Code generated by internal/gendrivers from " + conf + ". DO NOT EDIT.\n")
	b.WriteString("// Edit hurd.conf and run make gen to update.\n\n")
	b.WriteString("//go:build fat\n\n")
	b.WriteString("package main\n\nimport (\n")
	for _, p := range paths {
		fmt.Fprintf(&b, "\t_ %q\n", p)
	}
	b.WriteString(")\n")

	src, err := format.Source([]byte(b.String()))
	if err != nil {
		log.Fatal(err)
	}
	if err := os.WriteFile("drivers_gen.go", src, 0o644); err != nil {
		log.Fatal(err)
	}
	fmt.Printf("gendrivers: %d driver package(s) -> drivers_gen.go\n", len(paths))
}
